services:
  - type: web
    name: four-in-a-row-backend
    runtime: docker
    dockerfilePath: ./backend/Dockerfile
    dockerContext: ./backend
    envVars:
      - key: PORT
        value: 8080
      - key: DATABASE_URL
        sync: false
